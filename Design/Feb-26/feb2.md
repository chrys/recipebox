1. Recipes page 
1.1 Ensure that you indicate what are the mandatory fields in recipes when the user creates / edits a recipe. 
1.2 Show useful message when a user misses a mandatory field. 
1.3 Users can mark recipes using a star system. So a recipe they liked has five stars while a recipe they don't like as one star.
1.4 Create 5 recipes of each category: meat, beans, fish, chicken, pasta. Add images if you can to the recipes. These recipes should be public = TRUE
2. Admin Page
2.1  Create a third page under Calendar called Admin 
2.2 In this admin page the users can add/remove categories  
2.3 In the admin page the users can define the schedule, meaning which category of food for which day.  
3 Calendar 
3.1 Add button schedule current week: When user presses this button, the system should add a recipe for each day of the week according to the configuration of 2.3. This button should be enabled only when the current week does not have recipes. 
3.2 When a recipe s selected the current options are View Recipe and Remove. Add another option Replace Recipe. When this option is selected the recipe will be replaced with another recipe of the same category. 
